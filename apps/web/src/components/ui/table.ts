// Shared header-row styling for every data table in the app (Clientes,
// Casos, Configurações → Equipe). Single source so the shaded band + rule
// stays consistent instead of being copy-pasted per table.
export const TABLE_HEADER_ROW_CLASS = "border-b bg-muted/50 text-left text-muted-foreground";
