# 09: Clientes table — bold name column

**What to build:** The wireframe weights `ClientsTable.tsx`'s NOME/RAZÃO SOCIAL column bolder than the columns beside it (CPF/CNPJ, telefone, e-mail); the build renders it at the same weight as the rest of the row. One-line style fix — add the weight to that column's cell.

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] Client name column renders bold, other columns unchanged
- [ ] No layout shift/overflow introduced by the heavier weight at existing column widths
